{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "buildCommand": "npm install && npx webpack --config build/webpack.config.js --env production",
  "outputDirectory": "dist",
  "framework": null
}
